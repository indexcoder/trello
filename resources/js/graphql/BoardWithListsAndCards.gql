query BoardWithListsAndCards($id: ID!) {
    board(id: $id) {
        id
        title
        color
        lists {
            id
            title
            cards {
                id
                title
                order
            }
        }
    }
}
